# TripAgent

## 1. Project Overview
A trip planning application with features including: account management, accommodation and attraction lookup, itinerary management

## 2. Setup Instruction
python -m venv .venv
.venv/bin/python -m pip install --upgrade pip
.venv/bin/python -m pip install -r requirements.txt
## 3. Usage guide

