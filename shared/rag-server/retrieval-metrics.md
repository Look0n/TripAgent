# Retrieval Metrics

This file is reserved for retrieval evaluation results.

The supplied Lab 8 `rag_pipeline.py` does not automatically write retrieval metrics. Runtime RAG operations are written to `rag-audit.jsonl`; retrieval evaluation can be added through `rag_eval.py` when required.
